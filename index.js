const info = require('./lib/info')
const packages = require('./lib/packages')
const versions = require('./lib/versions')

module.exports = {
  info: info,
  packages: packages,
  versions: versions
}
